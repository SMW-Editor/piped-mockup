[package]
name = "piped-mockup"
version = "0.1.0"
edition = "2021"

[dependencies]
iced = { version = "0.12.1", features = ["image", "debug", "advanced"] }
bytemuck = "1.15.0"
glam = { version = "0.29.0", features = ["bytemuck"] }
